//given two linked lists with numbers, consider them as numbers and reverse them, add them and again reverse and make a new linked list.
//eg. l1=[2,4,3], l2=[5,6,4] thus l3=[7,0,8]

#include <iostream>
using namespace std;

class node
{
    public:
        int left, right;
        node*left= NULL;
        node*right=NULL;       
};

int main()
{
    int n,m;
    node*head=NULL;
}